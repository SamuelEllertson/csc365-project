Room
__RoomId__ 	int	primary key
MaxOccupancy	tinyint
RoomType	enum('single', 'double', 'twin')
BedType		enum('twin', 'twin XL,' 'double', 'queen', 'king', 'CA king')
BedCount	tinyint
Decor		enum('Modern', 'Industrial', 'Nautical', 'Scandinavian', 'Bohemian', 'Rustic')
Price		float

Reservation
__ReservationId__ int	primary key
UserId		int	references User.__UserId__
CardId		bigint	references CreditCard.__CardId__
CheckIn		date
CheckOut	date
Occupants	tinyint

-- there can be multiple rooms reserved for one reservation, so we need a separate table
RoomsReserved
ReservationId	int	references Reservation.__ReservationId__
RoomId		int	references Room.__RoomId__

User
__UserId__	int	primary key
FirsName	varchar
LastName	varchar
username	varchar
password	varchar  --Ignoring security; This is a class assignment, not a production project
type		enum('manager', 'normal')

CreditCard
__CardId__	bigint	primary key
balance		float
climit		float
